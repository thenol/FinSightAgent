# Impact Analysis V2 待开发清单

> 目标：以结构化因果图、证据绑定和情景分析替代单次自由文本影响总结。

## P0：数据契约与生命周期

- [x] 已新增并接入 `ImpactAnalysisOutputV2`（2.0.0）；因果图、情景、影响维度和质量报告会保存到 `analysis_payload`，旧字段由投影生成。
- [x] 已新增分析快照与质量报告字段、Alembic 迁移及 PostgreSQL/内存 Repository 映射。
- [-] 已实现 `draft → needs_review → approved → superseded` 基础状态流转，降级结果禁止批准；事务边界强化待完成。
- [x] 新增影响分析状态转换 API，支持整体审核状态变更。
- [-] 已将旧版本替代动作延后到新版本审核通过时；数据库事务封装和并发冲突测试待补齐。

## P1：证据化分析流水线

- [-] 已实现 Context Builder，接入 `as_of`、verified Claim、事实卡片、混合检索和数据可用性声明；结构化/关系/历史检索编排待扩展。
- [ ] 实现预期差识别；缺少基准预期时明确输出 unknown。
- [-] 已接入 `MechanismGenerator` 与 `ImpactCritic`，执行证据缺失、端点、循环边和目标图谱一致性检查；情景扩展与影响映射仍待进一步拆分。
- [ ] 每条因果边绑定支持/反对证据、推理类型、时滞、条件和失效触发器。
- [x] 已完成基础确定性 Quality Gate（悬空边、情景引用、目标路径、证据覆盖和无证据定量结论）；未来数据与 Claim 状态校验待接入 Context Builder。

## P1：产品输出

- [x] 前端已替换为 React Flow + ELK 分层画布，保留情景/时间过滤、路径聚焦和证据抽屉；旧版图通过只读适配器展示。
- [ ] 影响目标按收入、成本、利润率、现金流、估值、流动性和信用等维度展示。
- [ ] 支持草稿/待审核/已批准状态和版本差异对比。

## 本轮已实现：专业因果图编辑闭环

- [x] 新增 V2.1 图读取接口，旧 V1 链路按确定性规则转换为只读 V2 图。
- [x] 已批准分析不可直接修改；通过 drafts API 派生独立草稿，保留 `derived_from_id` 和版本审计关系。
- [x] 草稿图 PATCH 支持 `expected_revision` 乐观锁，冲突返回 409，质量门禁重新计算。
- [x] 新增个人布局快照 GET/PUT/DELETE，布局与分析语义版本分离。
- [x] 前端使用 React Flow + ELK Layered，节点类型、边置信度、方向和 MiniMap/Controls 可视化。
- [x] 升级为研究员视图：默认高置信度核心路径、方向/推理类型视觉编码、图例、关系详情侧栏、全屏与个人布局保存；旧版适配图包含影响对象节点。
- [x] 增加双层传导注解：图面显示机制/方向/置信度摘要，详情侧栏展示阶次、时滞、推理类型、成立/失效条件、上下游关系和证据引用。
- [x] 影响目标表“依据与解释”增加面向非经济学读者的通俗说明，明确方向、影响程度、预计时间和“分析推断不等于已发生结果”。
- [ ] 下一步：节点/边表单编辑器、低置信度/二阶路径展开、证据详情 API 与批量布局同步。

## P2：评估与扩展

## P1：多事件组合影响层（2026-08-17）

- [x] 新增标准化影响目标、事件关系、单事件贡献和组合快照领域模型及迁移 `20260817_0024`。
- [x] 组合聚合仅读取最新有效的 `approved` 分析；支持正/负总强度、净影响、方向反转、依赖降权和时间衰减。
- [x] 新增 `ImpactAggregationService`、Outbox 事件 `target_impact.recompute.requested.v1` 和 `impact-aggregation` Worker。
- [x] 新增目标影响查询、快照、重算和事件关系 API；支持全部标准目标类型。
- [x] 新增“目标影响”工作台，显示综合方向、净影响、正负贡献、置信度和主导事件。
- [x] 增加多事件方向反转、未批准排除、时间衰减和迁移兼容测试。
- [ ] 补充行业分类目录导入、目标名称歧义审核和跨事件关系审核页面。
- [x] 将多事件汇聚图、事件贡献瀑布图和时间轴接入目标详情页（首版只读工作台）。
- [ ] 将贡献计算因子、影响维度和情景结果规范化为不可变快照，并补充通用布局存储。

## P1：未来行业影响窗口（2026-08-17）

- [x] 新增 `ForwardImpactWindow`、`ForwardCatalyst`、未来贡献和未来时间点快照模型。
- [x] 支持 scheduled / conditional / hypothetical 三类未来事件，基准与压力情景隔离。
- [x] 实现窗口范围、`as_of`、时间粒度、概率加权、条件影响和时间曲线计算。
- [x] 新增前瞻窗口 API、Outbox 事件和 `forward-impact` Worker。
- [x] 前端增加行业前瞻页面，支持自定义日期窗口和未来影响时间点查看。
- [ ] 接入官方事件日历和条件指标触发器，完善催化剂审核页面。
- [ ] 将前瞻时间线升级为带不确定性带的可视化曲线和多事件 DAG。

## P1：全局未来事件研究日历（2026-08-18）

目标：统一展示未来事件、日期级有效影响和目标联动，避免前瞻窗口只能从单一目标进入。

- [x] 锁定全局研究日历、月/周/列表视图和“当日事件 + 当日有效影响”语义。
- [x] 确定官方源适配器 + 人工维护的数据策略，以及事件修订、改期、取消、实现的生命周期。
- [x] 新增规范化 FutureEvent / FutureEventRevision / FutureEventTargetImpact 数据模型，迁移 `20260818_0026` 已应用并兼容现有 ForwardCatalyst。
- [x] 新增未来事件范围查询、日期摘要、单日研究视图和事件详情/生命周期 API。
- [ ] 新增研究日历前端，支持日期选择、目标筛选、事件详情和目标影响联动。
- [ ] 接入央行、统计、交易所和公司日程官方适配器，并实现幂等、改期和取消处理。
- [ ] 增加未来事件转正式 Event 的实现流程与预测复盘。

- [ ] 建立公司事件与宏观事件专家标注集，评估证据覆盖、目标相关性、方向一致率和稳定性。
- [ ] 行情数据源确定后接入事件研究、超额收益和预测兑现跟踪。
- [ ] 完成功能稳定后再补齐组织级权限、敏感数据治理和对抗性安全增强。

## P1：多市场行情与市场展望（2026-08-18）

- [x] 参考 `stock` 项目的东方财富主源、AKShare 回退、交易日历和行情字段，定义 FinSightAgent 独立 Provider 契约。
- [x] 支持 A/H/US 市场标识、5 分钟/日线、快照、交易日历、供应商来源、可用时间和 `as_of`。
- [x] 完成东方财富适配器、AKShare 可选边界、主备回退和能力降级 API。
- [x] 完成市场快照/K线查询 API 和基础质量测试。
- [x] 增加首批 A/H/US 指数与代表 ETF 的版本化参考目录及供应商代码映射。
- [x] 增加按市场时区和交易时段返回的交易日历 API，并显式输出节假日源降级原因。
- [x] 增加基于标准行情契约的市场状态计算和趋势/波动/覆盖率 API。
- [ ] 增加证券主数据与供应商映射表，禁止生产查询依赖临时代码推断。
- [ ] 增加 ClickHouse 标准行情表、对象存储原始快照和 market-data Worker。
- [ ] 增加多市场交易日历、午休/半日市/夏令时和跨日会话处理。
- [ ] 增加市场状态特征、预期差、已定价程度和跨市场领先指标。
- [ ] 增加 `MarketOutlook`、`OutlookContribution`、`OutlookEvaluation` 及 1/3/5/20 日概率输出。
- [ ] 使用 walk-forward、purge/embargo 和概率校准完成真实行情回测。
- [ ] 增加市场展望、行业热力图、贡献下钻和预测兑现页面。
